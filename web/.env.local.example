# 도커 Postgres 접속 (scripts로 컨테이너를 만들 때 정한 비밀번호)
DATABASE_URL=postgresql://etf_advisor:CHANGEME@localhost:5433/etf_advisor
POSTGRES_PASSWORD=CHANGEME
# 세션 쿠키 서명 키 — openssl rand -hex 32
SESSION_SECRET=CHANGEME
# launchd 정기 조사 트리거 인증 — openssl rand -hex 24
CRON_SECRET=CHANGEME
