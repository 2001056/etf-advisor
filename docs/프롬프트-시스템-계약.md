# 프롬프트 ↔ 시스템 계약

프롬프트를 고칠 때 이 문서와 어긋나지 않는지 확인한다. 프롬프트 본문은 DB `prompts` 테이블에
저장되며, 마크다운 파일에서 한꺼번에 넣으려면 `pnpm --dir web run prompts:load <파일>`을 쓴다.

## 시스템이 자동으로 붙이는 것 (프롬프트에 다시 쓸 필요 없음)

| 에이전트 | 앞에 붙는 것 | 뒤에 붙는 것 |
|---|---|---|
| ① 정기 조사 | — | 조사 문서 표준 형식 (frontmatter + 고정 헤딩 7개 + 데이터 표 규칙) |
| ② 매입시점 조사 | — | **활성 카테고리 한 줄** + 같은 형식 지시 + **조사 대상 티커 목록** |
| ③ 추천 | **활성 카테고리 한 줄**, 카테고리별 현재 잔액(자산성장은 자리별 배정액 한 줄 더), **현재 보유 현황**, **실시간 시세 블록**, ②의 문서 전문(+`research_doc_id`), 이전 문서들(+각 `research_doc_id`) | 출력 규칙 요약 |

③은 `--output-schema`로 JSON을 강제한다.

## 활성 카테고리 — ②·③은 활성 카테고리만, 비활성 보유는 ④만 본다

**활성 카테고리 = 설정 화면의 월 충전액 > 0 인 카테고리**
(`activeCategories`, `src/domain/recommendation.ts`). 잔액은 보지 않는다 — 비활성 카테고리에
매도 차액·분배금 같은 1주도 못 사는 잔돈이 생겨도 활성으로 되살아나지 않는다.
충전액이 전부 0이면 세 카테고리 전부로 둔다. "매입 추천" 흐름은 입구에서 한 번만 정해 ②·③에 같은 값을 쓴다.

세 카테고리가 모두 활성이면 아래 동작은 모두 이전과 같다(출력 규칙 10줄·형식 지시문은
`scripts/check-recommend.ts`가 이전 원문 스냅샷과 비교한다). 예외 두 가지:

- ②·③ 프롬프트에는 `[이번 회차 활성 카테고리: …]` 한 줄이 항상 붙는다(세 개면 세 이름).
  ② 프롬프트의 입력 범위 문구는 이 줄을 가리킨다("시스템이 알려주는 활성 카테고리").
- 대시보드 시세(`getQuotes`, `src/domain/quotes.ts`) 변경은 카테고리와 무관하게 항상 적용된다 —
  ④·⑤ 문서를 시세 창에서 빼고, 창 안 문서에 값이 없는 **현재 보유(수량 > 0)** 종목은 데이터 표 행
  (`| 종목코드 |`)이 있는 과거 문서에서 채운다. 이렇게 채운 분배율이 최신 조사 문서보다 오래되면
  배당금 예상액 옆에 `(M/D 기준)`이 붙는다.

활성 카테고리가 일부일 때:

- ① 형식 지시문의 `## 3` 소제목은 활성 카테고리만 붙고, 데이터 표 예시 행도 첫 활성 카테고리 종목으로 바뀐다.
  데이터 표 `category` 허용값(세 가지)은 그대로다.
- ② 조사 대상 = 주입 창 문서의 **데이터 표에서 category가 활성인 행**의 티커 ∪ **활성 카테고리** 매입 이력 종목
  (`researchTickers`). category를 알 수 없는 frontmatter 전용 티커와 세 이름 밖 category 행은 빠진다.
  "매입 추천" 화면의 "이번에 재조사할 종목 N개"도 같은 함수(`tickersToResearch`)를 쓰고, 그 위에 활성 카테고리를 보여준다.
- ③ 잔액 줄은 세 카테고리를 그대로 보여주되 비활성 카테고리 줄 끝에 `(이번 회차 매수 안 함)`,
  [현재 보유 현황]의 비활성 카테고리 종목에는 `(이번 회차 매수 대상 아님)`이 붙고, [출력 규칙]의 picks 개수 문구가
  활성 카테고리 기준으로 바뀐다(`budgetLine`·`holdingLine`·`recommendOutputRules`).
- ③ 정규화(`normalizePicks`)에는 비활성 카테고리 잔액을 0으로(`activeBalances`) 하고 활성 목록(`active`)까지
  넘긴다. 모델이 비활성 카테고리 pick을 내도 건너뜀으로 저장되고
  (사유: "이번 회차 매수 대상이 아닌 카테고리라 매수하지 않음"), 고배당 건너뜀 재배분도
  비활성 카테고리로 흐르지 않는다.
- ③ 스키마의 picks에는 개수 제약이 없다. 모델이 활성 카테고리만 내면 그 카테고리 행만 저장된다.
- 비활성 카테고리 보유 종목은 ②·③에서 빠지고 **④ 보유 점검만** 계속 본다.
  ④·⑤의 입력과 형식 지시문은 활성 카테고리와 무관하게 바뀌지 않는다.

## ③ 입력의 실시간 시세 블록

② 문서의 `price`는 조사 시점 스냅샷이라 "이 배정액으로 몇 주를 살 수 있는가"가 어긋난다.
그래서 프롬프트를 **조립하기 전에** 실시간 시세를 받아 ② 문서 앞에 블록으로 넣는다(`realtimePriceBlock`).

- 대상 = ② 최신 문서의 데이터 표에서 **활성 카테고리**인 행의 티커 ∪ **활성 카테고리** 보유 종목(`researchTickers`).
- 형식: `[실시간 시세 — HH:MM KST 기준, <장 상태>, 매수 가능 여부와 ref_qty는 이 값으로 판단]` 뒤에
  `- 티커: N원 (HH:MM)`, 못 받은 종목은 `- 티커: 실시간가 없음`. 머리줄 시각은 받은 시세 중 가장 늦은
  기준 시각(하나도 없으면 조회 시각)이고, 종목 줄의 괄호는 그 종목의 체결 시각이다.
- **여기서 "시각"은 조회 시각이 아니라 종목별 마지막 체결 시각이다**(거래소 기준, 네이버 응답의
  `localTradedAt`. 값이 없거나 못 읽으면 그 종목만 조회 시각으로 대체된다 — `src/server/livePrice.ts`).
  머리줄 시각은 그중 가장 최신이고, 그래서 장 상태를 함께 적는다 — 장 마감 뒤에는 모든 종목의
  시각이 종가 시각이라 "지금"이 아니다. 표기는 전부 KST다.
- 장 상태는 받은 시세가 **전부** `marketStatus="OPEN"`일 때만 `장중`, 하나라도 아니면 `장 마감 후 종가`,
  하나도 못 받았으면 `장 상태 확인 불가`다. 종가를 장중 값처럼 읽지 않게 하려는 표시다.
- [출력 규칙]도 같이 바뀐다 — `ref_price`는 **이 블록 값만** 쓴다. 실시간가가 없는 종목은 이번 회차
  후보에서 제외하고, ② 문서 `price`로 대체하지 않는다.
- 정규화 뒤 `applyRealtimePrices`가 **최종 권위**로 한 번 더 기준가를 덮어쓴다. 캐시가 30초라
  ③ 실행(수 분) 뒤에는 대개 새로 받은 값이다. 모델이 본 값과 저장 값이 다를 수 있고 최종 권위는 정규화 뒤 값이다.
- **실시간가는 필수다.** 매수 행인데 이 단계에서 실시간가를 못 얻었거나(조회 실패), ② 가격과 30% 넘게
  벌어져 버렸거나, 실시간가로는 1주도 못 사면 그 행을 건너뜀으로 바꾼다 — 조사가로 대체하지 않는다.
  종목·가격을 비우고 수량을 0으로 두며, 사유(`… — 이번 회차 제외`)를 `rationale`과 실행 노트에 남긴다.
  그 자리 몫은 배정액 그대로 이월된다. 옛 갈아타기 행(`sell_ticker`가 있는 행)은 예전 규칙 그대로 둔다.
  비교 구간이 ②→③ 수 분이라 30% 초과는 실질적으로 오파싱만 걸린다. 대가는 그 회차 그 자리를 비우는 것이다.

## 자산성장 두 자리 — 공격적 성장 / 안정적 성장

**자산성장만** 한 종목이 아니라 성격이 다른 두 자리로 나눠 담는다
(`GROWTH_ROLES`·`ROLE_CATEGORY`, `src/domain/recommendation.ts`). 배당성장·고배당은 예전처럼 1종목이다.

- 자리는 `role="aggressive"`(변동이 크더라도 장기 기대 수익이 높은 성장 지수)와
  `role="stable"`(여러 업종에 분산하여 특정 업종·종목 의존도를 낮추는 주식 지수) 둘뿐이다. 모델은 자리마다 ETF를 하나씩 고른다.
- **비중은 모델이 정하지 않는다.** 설정(`settings.growth_roles`, 기본 `{aggressive: 0.5, stable: 0.5}`)에서 읽어
  시스템이 채운다. 저장값이 깨져 있으면(0 이하·1 이상·합이 1에서 0.02 넘게 벗어남) 기본 50:50으로 되돌린다
  (`normalizeRoleWeights`) — 깨진 설정 하나가 배정액을 잔액 밖으로 밀어내면 안 되기 때문.
- 자리별 `budget_krw = floor(자산성장 배정액 × 그 자리 비중)`(`seatBudget`). 내림이라 두 몫의 합은 배정액을
  넘지 않고, 남는 잔돈은 이월된다. `ref_qty`는 그 자리의 예산으로만 계산한다. 같은 식으로 계산한
  `roleBudgetLine`이 ③ 입력의 자산성장 잔액 줄 아래에 자리별 배정액을 한 줄 더 붙인다.
- **한 자리를 건너뛰거나 pick 자체가 없어도 남은 자리가 그 몫을 먹지 않는다** — 자리에 앉은 pick은 언제나
  자기 자리 몫만 받는다(예외 없음). 건너뛴 자리의 몫은 자산성장 잔액으로
  이월되어 다음 회차에 다시 자리 비중대로 나뉜다. 다른 카테고리로는 가지 않는다.
  자리가 비면 사유("자산성장 자리 N개가 비어 그 몫은 자산성장 잔액으로 이월")를 로그에 남긴다.
- 정규화(`normalizePicks`)가 모델 출력을 자리에 앉힌다. 자리를 명시한 pick이 먼저 앉고, role이 없는 pick이
  남은 자리를 채운다(사유를 로그에 남긴다). 같은 자리에 둘이 오면 먼저 나온 것만 쓰고, 자리보다 많이 오면 버린다.
  두 자리에 **같은 종목**이면 **공격적 성장 자리만 남고 안정적 성장 자리는 비운다**(사유 "두 자리에 같은 종목 —
  안정적 성장 자리 비움"). 모델이 어느 자리를 먼저 냈는지와 무관하다 — 코드가 `GROWTH_ROLES` 순서로 고른다.
  남은 자리는 자기 자리 몫(공격 비중)만 받고, 빈 자리 몫은 이월된다 — 한 종목에 잔액을 몰아주지 않는다.
- **빈 자리는 건너뜀 행으로 저장된다.** 자산성장이 활성이고 자리가 하나라도 찼는데 나머지가 비면,
  `normalizePicks`가 그 자리마다 `recommendations` 행을 하나 더 만든다(`category=asset_growth`,
  `role`=그 자리, `weight`=그 자리 비중, `budget_krw`=그 자리 몫, `ticker`·`ref_price`는 NULL,
  `ref_qty`=0, `skipped`=true, `rationale`=사유 — "두 자리에 같은 종목 — 안정적 성장 자리 비움" 또는
  "모델이 이 자리를 내지 않음"). 로그(`dropped`)에만 남기면 화면에서 그 자리가 통째로 사라져
  몫이 어디로 갔는지 보이지 않기 때문이다. `/recommend`는 기존 건너뜀 렌더로 "이 자리 몫 N원이
  자산성장 잔액에 남아 다음 회차에 다시 나뉩니다"를 보여준다. 건너뜀 행이라 기록 폼이 없고
  배정 소계(`!skipped`만 합산)와 수락 경로에는 영향이 없다.
  **자산성장 pick이 하나도 없으면 행을 만들지 않는다** — 모델 출력이 통째로 비었을 때
  "유효한 추천이 없습니다 + 원본"이라는 실행 노트가 사라지면 안 되기 때문이다.
- **자리 이름의 뜻은 형식 지시문이 문서에 넣어 준다.** `buildFormatInstruction`은 보이는 카테고리에
  자산성장이 있으면 `## 3` 안내 뒤에 `[자리 정의]` 블록을 붙인다(정의는 `GROWTH_ROLE_HINT` 한 곳에서 온다).
  데이터 표 `note` 첫머리에 자리(공격 / 안정 / **미정**)와 환헤지 여부를 적고, 전략을 충분히 확인하지
  못했으면 "미정"과 사유를 쓰며, 자산성장이 아닌 행에는 자리를 적지 않는다. 세 카테고리가 모두 활성이어도
  자산성장이 있으니 붙는다(그래서 `check:recommend`의 형식 지시문 스냅샷을 이때 갱신했다).
  자산성장이 없는 문서에는 붙지 않는다.
- `recommendations.role`·`recommendations.weight`(둘 다 nullable)에 저장된다. `purchases.recommendation_id`를
  거슬러 올라가면 보유 종목이 어느 자리인지 알 수 있고(`holdingRoles`), **⑤ 종목 정리**는 그것을 받아
  "자리가 서로 다른 두 종목은 통합 대상이 아니다"로 판단한다. 자리 도입 전 매입과 수기 매입은 자리를 알 수 없어
  판단 보류로 표기된다. 자산성장이 **서로 다른 자리 하나씩**으로만 차 있으면 ⑤는 아예 실행하지 않는다(`needsReview`).
- **후보는 해외 주식형만** — 코스피·코스닥·KRX 지수 등 국내 주식을 기초자산으로 하는 ETF는 ①·③ 프롬프트에서
  후보로 삼지 않는다(사용자 결정 2026-09-21). 환헤지 여부는 배제 사유가 아니라 표기 대상이다.
  **프롬프트 문구로만 막고 코드 필터는 두지 않았다.**
- [출력 규칙]의 개수 문구는 자리를 두지 않는 카테고리만 센다 — 자산성장이 활성이면
  "자산성장은 아래 자리 규칙을 따른다"로 넘기고 자리 규칙 2줄이 뒤에 붙는다. 자산성장이 비활성이면
  개수 문구는 이전 원문 그대로다(`scripts/check-recommend.ts`가 대조군으로 비교한다).
- 화면에서 자산성장 pick들을 한 박스로 묶는 조건은 **자리 표시가 붙은 pick이 있을 때뿐**이고,
  박스의 배정 소계는 건너뛰지 않은 행만 더한다(건너뛴 행의 `budget_krw`는 이월액이라 더하면 잔액을 넘는다).

회귀 테스트: `pnpm run check:recommend`(자리 배정·분할·대조군), `pnpm run check:consolidate`(⑤ 지시문 자리 표시),
`pnpm run check:accept`(두 자리를 차례로 기록해도 잔액 검증 통과, 별도 DB).

## ③ 출력 스키마 (필드명이 정확히 맞아야 함)

```jsonc
{ "picks": [ {
  "category": "배당성장 | 자산성장 | 고배당",
  "action": "buy | skip",
  "role": "aggressive | stable | null",  // 자산성장 pick만. 다른 카테고리는 null
  "ticker": "446720 | null",       // skip이면 null
  "etf_name": "…| null",           // skip이면 null
  "budget_krw": 350000,            // 시스템이 준 잔액
  "ref_price": 14110,              // skip이면 null. 정수(원)
  "ref_qty": 24,                   // floor(budget ÷ price). skip이면 0
  "rationale": "…",
  "source_doc_ids": [1, 2],        // 주입된 research_doc_id 중에서만
  "source_urls": ["https://…"]
} ] }
```

- `additionalProperties: false` — **스키마에 없는 필드를 내면 실패한다.**
- 서버는 `budget_krw`를 모델 값이 아니라 자기 잔액으로 덮어쓴다(안전장치).
  자산성장은 자리 비중으로 나눈 몫을 쓴다(위 "자산성장 두 자리" 절).
- `role`은 자산성장 pick에만 쓴다. 자리 비중(`recommendations.weight`)은 **모델이 보내지 않고** 시스템이 채운다.
- `source_doc_ids`는 실제 주입된 ID만 남기고 나머지는 버린다.
- `action=skip`이거나 ticker·가격·수량이 성립하지 않으면 건너뜀으로 저장된다.
- **`ref_price`는 정규화 후 실시간가로 교체될 수 있으며 `research_price`에 원값을 남긴다** —
  교체되면 `price_source='realtime'`·`priced_at`(기준 시각)이 함께 기록되고 `ref_qty`도 같은 식으로 다시 계산된다.
  순수 매수 행은 실시간가를 못 얻으면 제외된다(위 절). `price_source='research'`로 남는 경우는 `sell_ticker`가 있는 옛 갈아타기 행뿐이다.
  매도가(`sell_ref_price`)도 같은 방식이며 원값은 `sell_research_price`에 남는다.
- **③은 갈아타기를 만들지 않는다.** 스키마의 `action`은 `buy`/`skip` 두 가지뿐이고 `sell_*` 필드도 없다.
  옛 형식으로 `action="switch"`가 와도 매수로만 받고 사유("③은 갈아타기를 쓰지 않음 — 매수로 처리: …")를
  로그에 남긴다. 매도·교체 검토는 ④, 중복 보유 정리는 ⑤가 맡는다.
  `recommendations`의 `sell_*` 컬럼과 `/switch` 화면(`executeSwitch`·`cancelSwitch`)은 **그대로 둔다** —
  과거 행 조회·표시 호환(수락은 막음)과, ④ 권고를 보고 사람이 직접 기록하는 경로다.
  `sell_ticker`가 있는 옛 행은 `acceptRecommendationAction`이 거부하고 `/recommend`도 폼 대신 `/switch` 안내를 띄운다.

## 조사 문서(①·②) 계약

- 헤딩 7개 문구 고정. `## 4. 데이터 표`의 컬럼은 **15개**를 이 순서 그대로:
  `ticker|name|category|price|dist_yield|total_return_1y|return_basis|nav_trend|yield_basis|premium|note|aum_bn|cost_pct|turnover_bn|mdd_1y_pct`
  (`DATA_TABLE_COLUMNS`, `src/domain/docFormat.ts`).
- `price`는 원 단위 정수, `dist_yield`·`premium`은 % 기호 없는 숫자.
- 뒤의 **핵심 증거 4열**(2026-09-21 추가)은 ③의 코드 게이트가 읽는 값이다.

  | 컬럼 | 뜻 | 단위·표기 |
  |---|---|---|
  | `aum_bn` | 순자산총액 | **억원 정수**. 1조 2,000억 → `12000`. 50억 미만이면 이 도우미의 내부 보수 기준에 걸린다(아래 "순자산 50억") |
  | `cost_pct` | **실부담비용** = 총보수 + 기타비용 + 매매·중개수수료율 | % 숫자. 못 찾으면 차선을 넣는다. 값을 넣었으면 `note`에 **비용 플래그**를 반드시 적는다 — 실부담비용이면 "비용:실부담", 합성총보수(= 총보수 + 기타비용)면 "비용:합성총보수", 총보수만이면 "비용:총보수만". `NA`로 두지 않는다. 셋 다 못 찾았을 때만 `NA` |
  | `turnover_bn` | 최근 평균 거래대금 | 억원, 소수 1자리 허용 |
  | `mdd_1y_pct` | 최근 1년 최대 낙폭 | **음수** % 숫자 |

- 네 열 모두 `NA`를 허용한다. `REQUIRED_COLUMNS`에는 넣지 않았다 —
  넣으면 옛 문서가 전부 형식 위반으로 떠서 경고가 의미를 잃는다.
- **`cost_pct`의 비용 폴백과 비용 플래그(2026-09-21 외부 검토 6차 → 7차에 두 갈래로 분리 → 8차에 누락 처리 반전)** —
  실부담비용을 못 찾았다고 `NA`로 두면 게이트가 그 종목을 통째로 버려 과잉 기권이 된다.
  그래서 차선이라도 확인되면 그 값을 넣고 `note`에 무엇을 넣었는지 플래그로 적게 했다.
  **`cost_pct`가 `NA`가 아니면 플래그는 셋 중 하나를 반드시 적는다**(8차) — 7차까지는
  "실부담비용이면 플래그를 적지 않는다"였는데, 그러면 **표기를 빠뜨린 행이 실부담비용으로 읽혀**
  가장 비싼 값이 가장 싼 값으로 둔갑한다. 이제 누락은 최하 등급(총보수만)으로 떨어진다.

  | `note`의 비용 플래그 | `cost_pct`에 들어온 값 | 실부담비용과의 거리 |
  |---|---|---|
  | `비용:실부담` | 실부담비용 | — |
  | `비용:합성총보수` | 총보수 + 기타비용 | 매매·중개수수료만 빠졌다 |
  | `비용:총보수만` | 총보수 | 기타비용·매매·중개수수료가 빠졌다 |
  | (없음 / 셋 중 하나가 아님) | 알 수 없다 → **총보수만으로 취급** | 누락은 최하 등급으로 떨어진다 |

  `checkRow`(`src/domain/dataChecks.ts`)가 `cost_pct`는 있는데 플래그가 없는 행을 **주의**로 잡는다
  ("비용 기준 표기 누락 — 총보수만으로 취급"). 문서 저장 시 경고 목록에 뜬다.

  **코드 게이트(`gateCoreEvidence`)는 `NA` 여부만 보므로 변경이 없다** — 어느 값이 들어왔는지는
  `note`를 읽는 ③ 프롬프트와 ④의 후보 줄(`costBasisFromNote`)만 안다(아래 "프롬프트로만 강제되는 규칙").
- **`note` 앞부분의 순서(2026-09-21 외부 검토 7차)** — 형식 지시문과 ①② 프롬프트가
  `자리 / 환헤지 / 비용 플래그` 순으로 ` / `로 구분해 적으라고 지시한다(없는 칸은 건너뛰고 순서만 지킨다).
  **코드는 그 순서에 기대지 않는다** — `roleFromNote`·`costBasisFromNote`는 여전히 `,` `/` `;` `|`를 모두
  구분자로 받고 앞쪽 조각을 훑는다. 지시문은 모델이 읽기 쉬운 모양을 고정할 뿐이다.
- **옛 10컬럼 문서 호환**: 헤더에 네 열이 하나도 없으면 값은 전부 `null`이고
  행에 `hasEvidenceColumns: false`가 붙는다. 형식 경고는 뜨지 않는다.
  ③ 게이트는 이 표시를 보고 사유에 "구 형식 문서"를 덧붙인다.
- **형식 지시문의 예시 행은 가짜다.** 자산성장 예시 행은 `999999 | 예시 ETF(가짜 코드)`이고
  표 규칙에 "이 행은 예시이며 실제 표에는 넣지 않는다"가 붙는다 — 모델이 예시 종목을
  실제 후보로 베껴 쓰는 것을 막는다. 파서의 `isPlausibleTicker`는 건드리지 않았다
  (`999999`는 여전히 유효한 티커 형태다 — `check:parser`의 유령 행 회귀가 그 값을 쓴다).
- **`return_basis` — 수익률 기간 기준(2026-09-22 외부 검토 10차에 추가, 14 → 15열)**.
  `total_return_1y` 칸에는 총수익률 숫자를 그대로 두고, **그 숫자가 어느 기간을 잰 값인지는 이 열이 정한다.**

  | 값 | 뜻 | 언제 |
  |---|---|---|
  | `1y` | 최근 12개월 | 상장 12개월 이상이면 이 값만 허용한다 |
  | `since_listing:N` | 상장 N개월(12 미만)의 상장 이후 총수익률 | 상장 12개월 미만 |
  | `NA` | 상장 이후 실적도 확인하지 못함 | 이때는 `total_return_1y`도 함께 `NA`(**둘 다 NA**) |

  파서는 값을 소문자로 정규화해 `DataRow.returnBasis`에 담는다(`src/domain/docFormat.ts`).
  **옛 문서(이 열이 없는 14열·10열)는 빈 문자열**이라 ③ 게이트에서 "수익률 기간 기준" 미확인으로 걸린다.
  `REQUIRED_COLUMNS`에는 넣지 않았다 — 옛 문서가 형식 위반으로 뜨지 않게 한다.
- `total_return_1y`는 **NAV 기준·분배금 재투자 가정** 총수익률이고 기간은 `return_basis`가 정한다. 운용사·거래소 공시값을 우선 쓰고,
  없으면 계산 근거를 `note`에 적는다. 시장가 기준이거나 분배금을 현금으로 합산한 방식이면 `note`에 명시한다.
  **`NA` 금지의 예외** — 상장 12개월 미만이라 1년 수익률이 없으면 상장 이후 수익률로 대체한다
  (`return_basis`는 `since_listing:N`). 상장 이후 실적도 공시값·원자료 어느 것도 확인하지 못했을 때만
  둘 다 `NA`로 두고 사유를 `note`에 적는다(형식 지시문·①②에 같은 문장).
- **3년 연환산 총수익률은 표에 컬럼이 없다.** 확인했으면 `note`에 `3y_ann:14.2%` 형태로만 적는다
  (형식 지시문). ③이 `rationale`에 인용하고, ④ 후보 줄은 이 표기를 읽어 `3년연환산 N%`를 병기한다
  (`ann3yFromNote`, `src/server/watch.ts`). 기간이 다르므로 `total_return_1y` 칸에는 넣지 않는다.
- `nav_trend`는 **최근 12개월 고정**이다(6개월만 관찰했으면 `note`에). 값 도메인은 그대로 `up`/`flat`/`down`/`NA`.
- **확인 불가는 `NA`** — 파서가 `NA / N/A / 없음 / 미확인 / 확인불가 / - / TBD`를 정상 결측으로 인정하고 경고하지 않는다. 그 외 읽을 수 없는 값은 문서 화면에 형식 경고로 뜬다.
- `ticker`는 6자리. 5~12자 영숫자가 아니면 유령 행으로 보고 버린다(표가 두 개 이어붙었을 때 헤더가 새는 것을 막는 장치).
- 같은 티커가 두 번 나오면 첫 행만 쓰고 경고를 남긴다.

## 위험 기준 수치와 출처 (2026-09-22 외부 검토 10차에 정확화)

두 수치는 프롬프트(①②④)와 `src/domain/dataChecks.ts`에 함께 박혀 있다. **한쪽만 고치지 말 것.**

**순자산 50억** — 두 문장을 구분한다.

1. **KRX 관리종목 지정요건**은 **상장 1년 경과 ETF가 반기말 기준으로 신탁원본액과 순자산총액을
   모두 50억 미만으로 둘 때**이고, 다음 반기말까지 해소하지 못하면 상장폐지 대상이 된다.
   출처: 한국거래소 안내 <https://www.krx.co.kr/contents/LST/06/06010500/LST06010500.jsp>
   (7차까지 적혀 있던 "삼성자산운용 KODEX 가이드" 출처는 이것으로 교체했다).
2. **이 도우미의 기준은 순자산 하나만 본다** — `aum_bn < 50`이면 `checkRow`가 주의를 남기고
   ①은 후보에서 제외, ④는 warn으로 본다. 상장 경과 기간도 신탁원본액도 보지 않으므로
   **지정요건 자체가 아니라 그보다 보수적인 내부 기준**이다. 프롬프트도 이 두 문장을 나눠 적는다.

**괴리율** — **절댓값 기준**이다(음수 괴리율도 같은 선에서 잡는다).
`|괴리율| 5%` 초과면 warn(해외 ETF LP 호가 관리범위 밖), `10%` 초과면 danger 후보(투자유의종목 지정예고 요건).
`checkRow`의 `PREMIUM_ABS_MAX`·`PREMIUM_ABS_SEVERE`가 `Math.abs(premium)`으로 비교한다.
출처: 금융위원회 2026년 8월 안내 <https://www.fsc.go.kr/po010106/87512>
(6차에 붙였던 파이낸셜뉴스 기사 출처는 이것으로 교체했다 — ②·④ 프롬프트도 같이 고쳤다).

회귀 테스트: `pnpm run check:data`("순자산 하한" 절 — 내부 보수 기준·신탁원본액 문구,
"괴리율 2단계" 절 — 절댓값 대조군 `±4.9`·`-6`·`-12`).

## ③ 핵심 자료 게이트 (코드가 검증한다)

"핵심 항목 4개 중 하나라도 확인되지 않은 후보는 선택할 수 없다"는 v6까지 **프롬프트 문구뿐**이었다.
데이터 표에 열이 생겼으므로 이제 코드가 막는다 — `gateCoreEvidence`(순수 함수, `src/domain/recommendation.ts`).

- 대상은 **순수 매수 pick**뿐이다. 이미 건너뛴 pick과 티커가 없는 pick은 보지 않는다.
- 보는 값은 ② **최신 문서 데이터 표의 그 종목 행**이다. `normalizePicks`의 `ctx.researchRows`
  (티커 → `DataRow` 전체 맵)로 넘긴다 — `researchPrices`가 가격만 넘기던 것과 같은 경로다.
- 넷 중 하나라도 `null`/`NA`면 그 pick은 **건너뜀**이 되고 사유가 붙는다:
  `핵심 자료 미확인(②): 실부담비용·순자산총액` 형태. `dropped`(실행 노트)와 `rationale`(화면) 양쪽에 남는다.

  | 게이트 항목 | 표 컬럼 |
  |---|---|
  | 비용 | `cost_pct` |
  | 거래 품질 | `aum_bn`, `turnover_bn` |
  | 장기 총수익 | `total_return_1y` **와** `return_basis` (둘 다 non-NA여야 한다) |

  장기 총수익만 컬럼이 둘이다 — 숫자 하나로는 그 값이 1년치인지 상장 3개월치인지 알 수 없어
  다른 후보와 비교가 성립하지 않는다. 기준이 비면 사유는 `핵심 자료 미확인(②): 수익률 기간 기준`
  (`RETURN_BASIS_LABEL`, `src/domain/recommendation.ts`). 옛 문서는 이 열이 없어 항상 여기서 걸린다.

- **게이트 대상이 아닌 것**: `mdd_1y_pct`(하락 위험)와 추적차이·추적오차.
  없어도 매수를 막지 않고 `rationale`에 "하락 위험 미확인"을 남기게 한다(프롬프트).
- ② 표에 그 티커 행이 아예 없으면 사유는 `핵심 자료 미확인(②): ② 문서 데이터 표에 N 행이 없음`.
- 옛 10컬럼 문서면 값이 전부 `null`이라 건너뜀이 되고, 사유 끝에 `(구 형식 문서 — 핵심 증거 열이 없음)`이 붙는다.
- `ctx.researchRows`를 **안 주면 게이트가 돌지 않는다.** 옛 호출부와 `check:recommend`의 대조군이 그대로 통과한다.
- 목적 적합성(기초지수·운용 전략)은 표 컬럼이 아니라 본문으로 확인하는 항목이라 코드가 검증하지 않는다.

회귀 테스트: `pnpm run check:recommend`("핵심 자료 게이트" 절 — 항목별 FAIL, 구 형식 문서, 행 없음, 대조군 2종).

**건너뜀률 지표** — ③ 실행 노트 첫 줄에 `건너뜀 N/M 자리 (사유별 집계)`가 남는다(`skipTally`).
게이트가 과하면 매 회차 전부 건너뛰게 되므로, 그 판단의 근거가 되는 숫자를 남긴다.
완화 여부는 사람이 정한다(`docs/운영.md`).

## ④ 대체 종목은 ①이 이미 고른 목록에서만

④가 매도를 권할 때 웹 검색으로 새 종목을 발굴하면, ①의 후보 구성 규칙(해외 주식형·대표 상품·자리)을
거치지 않은 종목이 보유로 들어온다. 그래서 고를 수 있는 목록을 시스템이 정해 준다.

- 목록은 **최신 ① 문서(`type=scheduled`)의 데이터 표**에서 만든다(`latestScheduledDoc` → `replacementCandidates`,
  `src/server/watch.ts`). **활성 카테고리 행만** 담고, 자산성장 행은 `note` 앞쪽의 "공격/안정/미정"으로 자리를 붙인다.
  `roleFromNote`는 note를 `,` `/` `;` `|`로 끊어 **앞 3조각**에서 자리를 찾는다 — "비용:총보수만" 같은 표기가
  앞에 오더라도 자리를 놓치지 않게 하려는 것이다. 못 찾으면 `null`이고 블록에는 **"자리 미정"**으로 찍힌다
  (`undefined`가 찍히면 안 된다).
- 줄에는 **① 표의 수치 5개**가 함께 실린다(`ReplacementCandidate`의 `aumBn`·`costPct`·`turnoverBn`·
  `totalReturn1y`·`price`). 값이 없으면 `NA`다. ④가 후보를 비교할 근거를 웹이 아니라 ① 표에서 받게 하려는 것이다.
- **총수익 칸에는 기간이 함께 찍힌다(2026-09-22 외부 검토 10차)** — `총수익 15%(1년)` /
  `총수익 15%(상장 3개월)` / `총수익 NA`(`returnCell`, `return_basis`를 읽는다).
  `return_basis` 열이 없던 옛 문서는 `(기간 미상)`이다. `note`에 `3y_ann:N%`가 있으면 `3년연환산 N%`가 뒤에 붙는다.
  블록 끝에 "기간이 다른 수익률로 우열을 매기지 마라"가 한 줄 더 붙는다.
- **줄 끝에 비고와 출처가 붙는다** — `비고 …`는 `note`에서 자리·환헤지·비용 플래그를 걷어낸 나머지
  60자다(`noteSummaryFromNote`, 남는 게 없으면 칸 자체가 없다). `출처는 ① 문서 research_doc_id N`은
  그 줄이 나온 ① 문서의 id로, `replacementCandidates(doc, active, researchDocId)`의 세 번째 인자다
  (안 주면 `null`이고 표기도 붙지 않는다 — 대조군).
- **비용 칸에는 기준이 함께 찍힌다(2026-09-21 외부 검토 7차)** — `costBasisFromNote`가 note 앞 4조각에서
  비용 플래그를 읽어 `ReplacementCandidate.costBasis`(`"total"`/`"synthetic"`/`"ter_only"`/`null`)를 정한다.
  `cost_pct`가 `NA`면 `null`이고 칸은 `비용 NA`, `비용:실부담`이면 `"total"`이라 `비용 0.23%`,
  나머지는 `비용 0.0888%(합성총보수)` / `비용 0.0062%(총보수만)`이다.
  **플래그가 없거나 셋 중 하나가 아니면 `"ter_only"`**라 `(총보수만)`으로 찍힌다(8차).
  이 칸만 `numCell`을 쓰지 않는다 — `toLocaleString`이 소수 3자리에서 끊어 `0.0062`를 `0.006`으로 만든다.
  같은 기준끼리만 비교하라는 규칙과 "(총보수만) 표기 후보를 '명백히 나은 대안' 근거로 쓰지 마라"는
  **④ 프롬프트**에 있다(코드는 표기까지만 한다).
- `buildAlertInstruction`에 `candidates`를 주면 [점검 대상] 바로 뒤에
  `[대체 종목 후보 — 이 목록에서만 고른다]` 블록이 붙는다
  (`- 자산성장 | 공격적 성장 자리 | 티커 이름 | 순자산 N억 | 비용 N% | 거래대금 N억 | 총수익 N%(1년) | 3년연환산 N% | 가격 N원 | 비고 … | 출처는 ① 문서 research_doc_id N`
  — `3년연환산`·`비고`·`출처`는 값이 있을 때만 붙는다).
  블록 끝에 "대체 종목 비교는 이 수치로 하고, 웹 검색은 최신 공시 확인에만 쓴다"가 붙는다.
  후보가 0건이면 "(후보 없음 — 대체 종목을 제시하지 말고 replacement는 비워 둔다)" 한 줄.
- `normalizeAlerts(raw, holdings, active, candidates)`가 목록 밖의 `replacement_ticker`를 **지우고**
  사유("대체 종목 후보 목록에 없어 제외: 보유티커 → 대체티커")를 `dropped`에 남긴다. 판정 자체는 살아남는다.
- `candidates`를 **안 주면** 블록도 붙지 않고 목록 검사도 하지 않는다(대조군 = v6 동작).
- **자리 일치는 프롬프트가 맡는다.** 코드가 보는 것은 목록 포함 여부뿐이다.

회귀 테스트: `pnpm run check:watch`("대체 종목 출처 제한" 절 + 대조군 3종,
"note 앞에 다른 표기가 와도 자리를 읽는다"·"비용 기준을 후보 줄에 표기한다"(대조군 2종 +
"플래그가 아예 없으면 ter_only")·"구 형식(10컬럼) ① 문서면 수치는 NA, 자리는 미정"·
"총수익 기간·3년 연환산 표기"(1년 / 상장 N개월 / 둘 다 NA + 3y_ann 대조군, 문서 id 대조군)),
`pnpm run check:data`("비용 기준 표기" 절 — 누락 → 주의, 플래그 3종 대조군, `cost_pct`가 `NA`면 검사 안 함).

## ④ stop_buying·sell → ③ 신규 매수 제외

④가 "이건 더 사지 마라"고 했는데 ③이 같은 달에 그 종목을 다시 고르는 구멍이 있었다.

- ③ 조립 시 `openAlerts()`(미해결 = `resolved_at IS NULL`)를 읽어
  `action`이 `stop_buying`·`sell`인 것만 추린다.
- 프롬프트에는 [현재 보유 현황] 뒤에 `stopBuyingBlock`이 한 줄로 붙는다 —
  `[보유 점검 경고 — 신규 매수 제외: 티커(신규 매수 중단 — 사유), …]`. 막을 것이 없으면 빈 문자열이다.
- 코드로도 막는다: `blockedBuyTickers` → `normalizePicks`의 `ctx.blockedTickers`.
  그 티커의 buy pick은 건너뜀이 되고 사유는 `신규 매수 제외(④ 보유 점검): 신규 매수 중단 — …`.
- `action=hold`(적립 중단 고정·`unknown` 고정 포함)는 막지 않는다 — 조치가 hold면 매수 금지가 아니다.

회귀 테스트: `pnpm run check:recommend`("④ 미해결 경고" 절 + 대조군 2종).

## ① 관찰 목록 연속성

①은 이전 문서를 받지 않아 관찰 목록이 회차마다 통째로 바뀔 수 있었다.

- `runResearch`의 `slot=weekly`일 때만 최신 ① 문서를 읽어 `priorWatchlistBlock`을 프롬프트 본문 뒤,
  형식 지시문 앞에 넣는다. ②는 받지 않는다(②는 조사 대상 티커 목록을 이미 받는다).
- 블록은 **활성 카테고리 행만** 담고 줄은 `- 카테고리 | 티커 이름 | note 앞 60자`(넘으면 `…`).
- ① 프롬프트의 "기존 대표 상품"·"기존 후보"가 이 블록을 가리키도록 문구를 바꿨다.
- 문서가 없거나(첫 실행) 활성 카테고리 행이 없으면 빈 문자열이라 블록이 붙지 않는다.
- **"새 후보 포함 조건"은 바꾸지 않았다**(사용자 결정 사항).
  제안: *분기마다 한 번은 목록 전체를 다시 검토한다* — 아직 반영하지 않았다.

회귀 테스트: `pnpm run check:recommend`("① 직전 관찰 목록 블록" 절 + 대조군 2종).

## 보유 현황·시세

**현재가는 대시보드를 열 때 네이버 금융에서 직접 가져온다** (`src/server/livePrice.ts`).

- 폴링 API(`polling.finance.naver.com/api/realtime/domestic/stock/{코드,코드,…}`)로 보유 종목 전체를 한 번에 조회. 실측 0.2초 내외.
- 30초 메모리 캐시, 2.5초 타임아웃. 실패하면 조용히 조사 문서 값으로 되돌아가고 화면에 **(문서)** 로 표시한다 — 대시보드가 외부 API 때문에 느려지거나 멈추지 않는다.
- 전일 대비(▲▼)와 장중/장마감 상태도 함께 보여준다.

**분배율(배당금 예상액)은 여전히 조사 문서에서 온다** — 실시간 소스에 없다.

- 최신 문서 우선이되 **필드 단위**로 채운다 — 최신 문서에 값이 NA면 그 항목만 과거 문서에서 가져오고 날짜를 함께 표시한다.
- 따라서 ②가 보유 종목을 빠뜨리면 그 종목의 배당 예상이 낡는다. ②의 "입력 티커 전원 포함" 규칙이 중요한 이유.
- 추천(③)은 **프롬프트 조립 전에** `getRealtimePrices`로 받은 값을 [실시간 시세] 블록으로 모델에게 주고,
  정규화 직후 같은 함수로 한 번 더 받아 `ref_price`를 **추천 시점의 체결가로 덮어쓴다**(최종 권위) —
  증권사 앱에 보이는 값과 맞추기 위해서다. 조사 가격은 `research_price`에 남는다.

회귀 테스트: `pnpm run check:live` (네트워크 필요).

## 고배당 건너뜀 시 재배분 (§2.2의 유일한 예외)

고배당을 **명시적으로 건너뛴 경우에만** 그 잔액을 배당성장·자산성장에 나눠 배정한다.

- 분배 비율은 기존 50:30을 그대로 쓴 **5:3** — 배당성장 `floor(고배당잔액 × 5/8)`, 자산성장은 나머지.
- 배정 금액과 수량은 **시스템이 계산한다.** 모델의 `budget_krw`·`ref_qty`는 참고만 하고,
  `ref_qty = floor(시스템 배정액 ÷ ref_price)`로 다시 계산한다.
- 받을 카테고리도 건너뛰면 그 몫은 배정하지 않고 고배당에 남아 이월된다.
- **실제 자금 이동은 매입을 기록할 때** 모자란 만큼만 일어난다(원장에 −/+ 두 행). 추천만 받고
  안 사면 고배당 잔액은 그대로다. 적게 사면 그만큼만 옮겨간다.
- 재배분이 발동하는 조건은 모델의 `action="skip"` **뿐이다.** pick이 아예 없거나
  `action="buy"`인데 가격이 빠진 경우(데이터 오류)는 재배분하지 않는다 — 실수로 돈이 옮겨가면 안 되므로.
- 배당성장·자산성장을 건너뛸 때는 재배분하지 않는다. 그 잔액은 자기 카테고리에 이월된다.

회귀 테스트: `pnpm run check:recommend`(배정 계산), `pnpm run check:transfer`(원장 이동, 검증용 DB 필요).

## ④ 보유 점검 출력 스키마

④는 데이터 표가 아니라 JSON(`--output-schema`)으로 판정을 받는다.

**④ 프롬프트에는 조사 문서 형식 지시문(`buildFormatInstruction`)이 붙지 않는다.** 프롬프트 본문 뒤에
붙는 것은 `buildAlertInstruction` 하나뿐이고, 조립은 `buildWatchPrompt`(순수 함수)가 한다. 한때
frontmatter·헤딩 7개짜리 마크다운 문서와 JSON을 동시에 요구했는데, ④가 남기는 문서 md는
판정 행으로 시스템이 만들기 때문에 그 요구는 지켜져도 쓰이지 않고 계약만 흐렸다.
`check:watch`가 조립 결과에 `## 1. 요약`·`[출력 형식`·frontmatter 블록이 없는지 본다.

프롬프트가 쓰는 말과 시스템 필드의 대응:

| 필드 | 값 | 프롬프트에서의 표현 |
|---|---|---|
| `severity` | `info` / `warn` / `danger` / `unknown` | 위험 아님 / 지켜볼 사항 / 구조적 위험 / 확인 불가 |
| `action` | `hold` / `stop_buying` / `sell` | 보유 유지 / 신규 매수 중단 / 매도·교체 |
| `replacement_ticker`·`replacement_name` | sell일 때만, 같은 카테고리(자산성장은 같은 자리) | 대체 종목 |
| `issue`·`rationale`·`source_urls` | 문제 요약·근거·출처 | — |

정규화 규칙: 보유하지 않은 티커의 판정은 버린다. `sell`인데 대체 종목이 다른 카테고리면
대체 종목만 지운다. `severity`나 `action`이 스키마 밖 값이면 **그 판정 행 전체를 버리고**
사유를 `dropped`에 남긴다.

### `severity=unknown` — 확인 불가

"판정에 필요한 핵심 자료를 확인하지 못함"이지 "문제 없음"이 아니다. `alert_severity` enum에
값이 하나 늘었고(DB는 `ALTER TYPE … ADD VALUE 'unknown'`), `normalizeAlerts`가 이렇게 다룬다.

- `action`이 `hold`가 아니면 `hold`로 고정하고 사유("확인 불가(severity=unknown)라 action=sell → hold 고정: …")를 `dropped`에 남긴다.
  확인하지 못한 것을 근거로 팔게 두지 않는다.
- `replacement`는 둘 다 비운다(사유 "확인 불가(severity=unknown) 판정이라 대체 종목 제외").
- `issue`에는 `FORCED_HOLD_PREFIX`를 붙이지 않는다 — 화면에 회색 "확인 불가" 뱃지로 그대로 읽힌다.
- 맥 알림은 `alertsToNotify`가 거른다. **`danger`만 알린다** — `unknown`은 위험을 확인한 게 아니다.
  대시보드에는 `info`가 아니므로 그대로 뜬다.
- 표시 순서는 enum 선언 순서가 아니라 `openAlerts`의 `SEVERITY_RANK`가 정한다
  (`danger` > `warn` > `unknown` > `info`). `unknown`이 enum 맨 뒤라 그냥 두면 "위험" 위로 올라온다.

시스템이 주입하는 [점검 대상] 줄에는 카테고리·티커·수량·평단가 뒤에 **자리**(`holdingRoles`로 찾은
공격적 성장 / 안정적 성장)와, 이번 회차 비활성 카테고리면 `(적립 중단 카테고리)`가 붙는다.

적립 중단 카테고리 보유는 **`severity`는 그대로 두고 `action`을 `hold`로 고정한다.** `normalizeAlerts`가

- `action`이 `sell`·`stop_buying`이면 `hold`로 바꾸고,
- `issue` 앞에 `[적립 중단 카테고리 — 매도 여부는 직접 판단] `(`FORCED_HOLD_PREFIX`)를 붙이고,
- 사유("적립 중단 카테고리라 action=sell → hold 고정: …")를 `dropped`에 남긴다.

모델이 이미 `hold`라고 했으면 접두도 사유도 붙이지 않는다 — 시스템이 고친 게 없기 때문이다.
대체 종목은 예전처럼 비운다(사유 "적립 중단 카테고리라 대체 종목 제외"). 재원도 대체 종목도 없는
카테고리에 매도를 권하면 사람이 따를 길이 없어서다. 화면에는 조치 뱃지가 "보유 유지"로 보이고,
그게 시스템 고정이라는 사실은 `issue` 앞의 접두로 읽힌다. 매도·이동은 사람이 결정한다.

## ⑤ 종목 정리 출력 스키마

⑤도 JSON이다. `suggestions[]` 각 항목:

| 필드 | 값 | 비고 |
|---|---|---|
| `category` | `div_growth` / `asset_growth` / `high_div` | 다른 값이면 그 제안 전체를 버림 |
| `tickers` | 묶을 보유 종목들 | 비보유 티커는 걸러지고, 걸러서 2개 미만이면 제안 폐기 |
| `keep_ticker`·`keep_name` | 남길 종목 (null 허용) | `tickers`에 없는 티커면 null 처리 |
| `overlap` | 겹침 근거 | 필수 |
| `cost_note` | 지금 옮길 때의 비용 | 필수 — 프롬프트가 "실행 비용" 절에서 요구 |
| `rationale`·`source_urls` | 판단 근거·출처 | — |

최상위에 `notes: string[]`가 하나 더 있다(required, 빈 배열 허용). 정리를 제안하지 않은 이유와
판단을 보류한 이유를 카테고리마다 한 줄씩 받는다. `normalizeSuggestions`가 문자열 아닌 항목과
빈 줄을 걸러 통과시키고, ⑤ 문서 md의 `## 검토 메모` 절에 기록된다 — **`suggestions`가 비어도
남는다.** 제안이 0건일 때 "왜 0건인지"가 유일하게 남는 기록이라서다. 표에는 저장하지 않는다.

## 프롬프트로만 강제되는 규칙 (코드가 검증하지 않음)

아래는 프롬프트 문구가 전부다. 코드에 대응하는 검사가 없으므로 모델이 어기면 그대로 통과한다.
문구를 지울 때 무엇이 사라지는지 알고 지울 것.

- **해외 주식형 한정** — 국내 주식형 배제는 ①·③ 프롬프트 문구뿐이고 코드 필터가 없다.
- **같은 기초지수 판별** — 기초지수 코드는 어디에도 저장하지 않는다. "두 자리에 같은 지수를 넣지 마라",
  "같은 지수는 대표 1개만 후보로", "1주 가격이 자리 몫을 넘으면 같은 지수의 다른 상품을 먼저 본다"는
  전부 모델의 판단에 맡긴 규칙이다. 코드가 막는 것은 **같은 티커**뿐이다(정규화가 안정적 성장 자리를 비운다).
  비대표 상품은 데이터 표에 행으로 남고(① 프롬프트) 파서는 행 수를 제한하지 않는다 — 그래야
  ②의 재조사 대상(`researchTickers`)과 ③의 선택 후보로 이어진다.
- **③ 채점 가중치와 0점 처리** — 100점 만점의 내부 비교 기준(목적 적합성 25 / 비용·추적 20 / 거래 품질 15 /
  장기 총수익·하락 위험 15 / 분산도 10 / 운용 이력 5 / 카테고리 고유 10)과, 보조 항목이 미확인이면 0점으로
  두고 분모는 100점 고정한다는 규칙. 채점은 모델 내부에서만 일어나고 저장되지 않는다.
  **핵심 항목 4개의 결측 판단은 더 이상 여기 없다** — 위 "③ 핵심 자료 게이트" 절에서 코드가 검증한다.
  (목적 적합성만은 표 컬럼이 아니라 본문으로 확인하는 항목이라 여전히 프롬프트 문구뿐이다.)
- **③ 장기 총수익을 무엇으로 보는가** — 3년 연환산 총수익률이 있으면 그것을 우선(표 컬럼이 없어
  `note`의 `3y_ann:N%` 표기로만 온다), 없으면 `total_return_1y`를 `return_basis`가 말하는 기간대로 읽는다.
  어느 것을 썼는지 `rationale`에 적는다. **후보 자격과 장기 점수는 분리한다**(10차) —
  `since_listing` 후보는 게이트를 통과해 후보 자격은 있으나 "장기 총수익과 하락 위험" 15점을 **0점 처리**하고
  관찰 기간을 `rationale`에 적는다. 기간이 다른 수익률끼리 숫자로 우열을 매기지 않는다
  (3년 연환산·1년·상장 이후는 각각 같은 기준끼리만). **코드는 `return_basis`가 비었는지만 보고
  0점 처리 여부는 검증하지 않는다.**
- **③ 시장 전망 금지** — 지수 방향·환율·금리 예상을 선택이나 건너뜀의 근거로 쓰지 않는다.
  건너뜀은 후보 자료·가격·거래 가능성 문제에 한한다. 코드는 건너뜀 사유 문자열을 읽지 않는다 —
  대신 `skipTally`가 건너뜀률을 실행 노트에 남겨 사람이 보게 한다.
- **④ 대체 종목의 자리 일치** — 자산성장 보유의 대체 종목은 같은 자리에서 고른다.
  코드는 후보 **목록 포함 여부**만 본다(위 "④ 대체 종목" 절). 자리까지는 보지 않는다.
- **① 자리당 후보 3개는 대표 후보 기준** — 같은 지수의 비대표 상품은 이 수에 넣지 않고 데이터 표에만
  행으로 추가한다. 표의 행 수는 코드가 제한하지 않는다.
- **기준이 다른 값은 채점하지 않는다** — `total_return_1y`가 시장가 기준·현금 합산이거나 `nav_trend`가
  6개월 관찰이면 ② 문서의 `note`에 그렇게 적히는데(형식 지시문), ③은 그 값을 미확인으로 취급한다.
  `note`는 자유 문자열이라 코드가 읽지 않는다.
  **이 기준 불일치 판단(시장가·현금 합산·6개월)은 `note`를 읽는 모델의 자기 신고로 남는다** —
  모델이 `note`의 표기를 무시해도 코드는 알 수 없다.
  **기간만은 예외다**(10차) — `return_basis` 컬럼이 생겨 1년치인지 상장 이후인지는 코드가 읽는다.
  다만 코드가 보는 것은 그 값이 **있는지**까지이고, 기간이 다른 값을 나란히 비교했는지는 여전히 프롬프트 몫이다.
- **③ 비용 항목의 상한 처리** — ③은 `note`의 비용 플래그를 보고 비용 항목을 확인 완료로 보되
  "비용과 추적 품질" 20점에 상한을 둔다: **"비용:실부담"은 상한 없음(20점), "비용:합성총보수"는 3/4(15점),
  "비용:총보수만"은 절반(10점)**. **플래그가 없거나 셋 중 하나가 아니면 총보수만과 같은 10점 상한**(8차).
  상한 규칙은 ③ 프롬프트의 "핵심 항목 최소 증거" 한 곳에만 두고, 채점 절에서는 그리로 참조만 한다(8차).
  `rationale`에는 무엇까지 확인한 값인지와 실부담비용 미확인을 적고,
  비용 수치의 직접 비교는 **같은 기준끼리만** 하며 기준이 다르면 그 사실을 `rationale`에 명시한다.
  `gateCoreEvidence`는 `cost_pct`가 `NA`인지만 보므로 이 구분을 **코드가 검증하지 않는다** — 위 "`cost_pct`의 비용 폴백과 비용 플래그" 참조.
- **④의 상환 경로** — 상환 예정이면 `hold`를 기본으로 하되 "거래정지 전 매도"와 "상환 대기" 두 경로의
  일정·거래 가능 여부·가격(NAV 대비)을 `issue`에 적어 사람이 고르게 한다. 시장 매도가 불가능하면 `sell`을 권하지 않는다.
- **④의 부분 결측** — 일부 자료 부족이 확인된 위험을 덮지 않는다. 미확인 항목은 `issue`에 "확인 불가: …"로
  따로 적고, 종목 전체를 `severity=unknown`으로 두는 것은 확인된 위험이 없고 핵심 자료도 확인하지 못했을 때다.
  `unknown`을 고를지 `warn`을 고를지는 모델의 판단이다 — 코드는 고른 뒤의 결과(action=hold, replacement 비움,
  알림 제외)만 강제한다.

## 프롬프트를 바꿀 때 깨지기 쉬운 것

1. 데이터 표 컬럼명·순서를 바꾸면 파싱이 통째로 실패한다.
2. ③의 필드명을 바꾸면(`ref_qty`를 `qty`로 등) 스키마 위반으로 실행이 실패한다.
3. ③이 카테고리를 "배당성장 " 처럼 공백 포함으로 내면 버려진다(서버가 trim하지만 표기 자체는 셋 중 하나여야 함).
4. ①이 카테고리를 세 가지 외 이름으로 쓰면 데이터 표에서 분류가 어긋난다.

## 반영 이력

- 2026-08-27: 프롬프트 v1 적재. 이때 시스템 쪽 5건을 프롬프트에 맞춰 수정 —
  ③ 스키마에 `action`/`budget_krw`/`source_doc_ids` 추가·`qty`→`ref_qty` 개명·skip 시 null 허용,
  `recommendations.ticker/etf_name` nullable화, ③ 프롬프트에 보유 현황·문서 ID 주입,
  파서의 `NA` 정상 인정, 형식 지시문에 6자리 티커·NA 표기 명시.
- 2026-08-31: ④·⑤ 프롬프트 작성·적재. 프롬프트의 판정 어휘(위험 3단계, 조치 3단계)를
  시스템 enum(`severity`/`action`)에 정확히 맞춤. ⑤에 "신규 매수만 모으기" 대안 제시를 의무화.
- 2026-09-18: 활성 카테고리(월 충전액 > 0) 도입. ②·③을 활성 카테고리로 좁히고(위 "활성 카테고리" 절),
  ② 프롬프트의 입력 범위 문구를 "시스템이 알려주는 활성 카테고리" 기준으로 바꿈. 바꾸기 전 프롬프트 본문은
  `data/tmp/` 아래에 백업한다(레포에 올리지 않음).
- 2026-09-21: 자산성장 두 자리(공격적 성장·안정적 성장) 도입. ③ 출력 스키마에 `role` 추가,
  `recommendations`에 `role`·`weight`(nullable) 컬럼 추가, 설정에 `growth_roles`(기본 50:50) 추가.
  ①·③ 프롬프트를 자리 기준으로 고치고 자산성장 후보를 **해외 주식형으로 한정**(사용자 결정).
  ⑤ 지시문에 자리 표시와 "자리가 다르면 통합 대상 아님" 규칙을 넣음(⑤ 프롬프트 본문은 그대로).
  ③ [출력 규칙]의 개수 문구는 자산성장이 활성일 때만 바뀐다 — HEAD의 "각각 정확히 한 번씩, 총 3개"는
  자산성장이 두 자리를 쓰면 숫자가 틀린 문장이 되기 때문. 자리 도입 시점에는 나머지 9줄을 그대로 두었다.
  바꾸기 전 프롬프트 본문은 `data/tmp/prompt-backup-2026-09-21/`에 백업(레포에 올리지 않음).
- 2026-09-21 (외부 검토 4차): ④ 출력 계약 충돌 제거 — ④ 프롬프트에서 조사 문서 형식 지시문을 떼고
  조립을 `buildWatchPrompt`로 분리했다(`docFormat.ts`의 `type: "watch"` 분기도 제거). ④ `severity`에
  `unknown`(확인 불가)을 추가하고 `alert_severity` enum에 값을 넣었다(`db:push`) — `unknown`은 `hold` 고정,
  대체 종목 비움, 맥 알림 제외(`alertsToNotify`), 대시보드에 회색 "확인 불가" 뱃지.
  ⑤는 `buildInstruction`의 "그건 정상적인 분산이다"를 "통합 대상이 아니다(분산 효과는 별도로 판단한다)"로
  고쳐 ⑤ 프롬프트 본문과 맞추고, 스키마에 `notes: string[]`를 넣어 제안 0건일 때의 사유를 남기게 했다.
  프롬프트는 ④(unknown 등급), ③(결측 규칙·핵심 4항목 최소 증거·같은 지수 대안은 선택 가능한 것만),
  ②(추적차이·최대 낙폭/변동성·상장 1년 미만 총수익률·note "미확인"), ①(자리당 3개는 대표 후보 기준)을 고쳤다.
  바꾸기 전 본문은 `data/tmp/prompt-backup-2026-09-21/{slot}.before-v6.md`, 최종본은
  `data/tmp/prompt-review-2026-09-21/{slot}.v6.md`(레포에 올리지 않음).
- 2026-09-21: ③ 갈아타기 제거(`action`=`buy`/`skip`, 출력 스키마의 `sell_*` 제거)로 [출력 규칙] 꼬리가
  9줄에서 6줄로 줄었다. ③ 프롬프트 앞에 실시간 시세 블록을 주입하고(위 "③ 입력의 실시간 시세 블록" 절)
  `ref_price`·`ref_qty` 기준을 그 블록으로 옮겼다. 자산성장 잔액 줄 아래에 자리별 배정액 줄(`roleBudgetLine`)을 붙였다.
  `research_price`에는 ② 최신 문서 표의 가격을 넣어 ② 오파싱 탐지(`isDrift`)의 기준을 되살렸다.
- 2026-09-21: **실시간가 필수**로 통일. 실시간가를 못 얻었거나 ② 가격과 30% 넘게 벌어졌거나 1주도 못 사면
  조사가로 대체하지 않고 그 행을 이번 회차에서 뺀다. 실시간 시세 블록에 종목별 체결 시각과 장 상태를 넣었다.
  두 자리에 같은 종목이 오면 합쳐 전액을 주던 경로를 없애고 안정적 성장 자리를 비운다.
  ④의 보유 주입에 자리·적립 중단 표시를 넣고, 적립 중단 카테고리 보유의 대체 종목은 시스템이 비운다.
  `total_return_1y`·`nav_trend`의 산출 기준과 관찰 기간을 형식 지시문과 ①·②·④ 프롬프트에 고정했다.
  ①·②·③·④·⑤ 프롬프트 본문도 같은 회차에 고쳤다(백업 `data/tmp/prompt-backup-2026-09-21/*.before-v4.md`,
  최종본 `data/tmp/prompt-review-2026-09-21/*.v4.md` — 둘 다 레포에 올리지 않음).
- 2026-09-21: 외부 검토 3차 반영(프롬프트 v5). 코드 3건 —
  ④ 적립 중단 카테고리 보유의 `action`을 `hold`로 고정하고 `issue`에 접두를 붙였고(위 "④" 절),
  형식 지시문에 `[자리 정의]`를 주입했으며(위 "자산성장 두 자리" 절 — 형식 지시문 스냅샷 갱신),
  빈 자리를 건너뜀 행으로 저장해 화면에서 그 자리 몫이 보이게 했다.
  프롬프트 4건 — ① 같은 지수 비대표 상품을 데이터 표에 남기고 "지수 2개 이상"을 목표로 낮춤,
  ② 자리 표기를 형식 지시문의 `[자리 정의]`에 위임하고 "미정"을 허용,
  ③ 결측 점수 규칙 교체·기준 불일치 값 채점 금지·1주 가격 초과 시 같은 지수의 다른 상품 우선,
  ④ 상환 예정의 두 경로 제시·부분 결측이 확인된 위험을 덮지 않게 함. ⑤ 본문은 그대로다.
  백업 `data/tmp/prompt-backup-2026-09-21/*.before-v5.md`, 최종본 `data/tmp/prompt-review-2026-09-21/*.v5.md`,
  ④·⑤ 시스템 지시문 사본 `data/tmp/prompt-review-2026-09-21/sys-watch.md`·`sys-consolidate.md`
  (모두 레포에 올리지 않음).
- 2026-09-21 (외부 검토 5차): **프롬프트로만 막던 규칙 3건을 코드로 옮겼다.**
  데이터 표에 핵심 증거 4열(`aum_bn`·`cost_pct`·`turnover_bn`·`mdd_1y_pct`)을 붙여 10컬럼 → 14컬럼이 됐고,
  ③이 그 행을 읽어 `gateCoreEvidence`로 매수 후보를 거른다(위 "③ 핵심 자료 게이트" 절).
  ④의 대체 종목은 최신 ① 문서의 데이터 표에서 만든 후보 목록 안에서만 고르게 했고(위 "④ 대체 종목" 절),
  ④의 미해결 `stop_buying`·`sell` 티커는 ③의 신규 매수에서 빠진다(위 "④ stop_buying" 절).
  ①에는 최신 ① 문서의 데이터 표를 `[직전 관찰 목록]`으로 되돌려 넣어 관찰 목록의 연속성을 줬다.
  `dataChecks`에 순자산 50억 미만 주의와 괴리율 2단계(5% LP 관리범위 / 10% 투자유의종목)를 넣었고,
  형식 지시문의 자산성장 예시 행을 가짜 코드(`999999 | 예시 ETF(가짜 코드)`)로 바꿨다
  (`check:recommend`의 형식 지시문 스냅샷을 이때 갱신했다).
  **수치 출처 주의** — 순자산 50억(관리종목 지정 요건)과 괴리율 5%·10%는 외부 검토가 제시한 값이다.
  2026-08-19 시행 기준으로 알려진 값이며 **규정 원문은 아직 확인하지 못했다.** 프롬프트에도 "규정 원문 확인 필요"로 적어 뒀다.
  **(10차에서 정정·확정)** 두 수치의 출처와 정확한 요건은 위 "위험 기준 수치와 출처" 절로 옮겼다 —
  50억은 한국거래소 안내, 괴리율은 금융위원회 2026년 8월 안내이고, 50억 규칙은 지정요건 자체가 아니라 내부 보수 기준이다.
  프롬프트는 ①(순자산 50억 후보 제외·실부담비용 정의·새 4열 표기·직전 관찰 목록 참조),
  ②(실부담비용 정의·괴리율 5%/10%·새 4열 표기), ③(핵심 4항목 최소 증거를 표 컬럼 기준으로 재작성·
  장기 총수익 3년 우선·시장 전망 금지·④ 경고 블록), ④(순자산 50억 warn·관리종목/상관계수 공시 danger·
  괴리율 2단계·대체 종목 후보 목록 한정)를 고쳤다. **⑤ 본문은 그대로다**(비용 정의를 ⑤ 기준으로 통일했다).
  바꾸기 전 본문은 `data/tmp/prompt-backup-2026-09-21/{slot}.before-v7.md`, 최종본은
  `data/tmp/prompt-review-2026-09-21/{slot}.v7.md`(레포에 올리지 않음).
  시스템 지시문 사본(`sys-*.md`)은 이번 회차에 갱신하지 않았다 — v6 시점 내용이다.
- 2026-09-21 (외부 검토 6차): **과잉 기권 완화와 ④ 후보 비교 근거.**
  `cost_pct`를 `NA`로 두던 규칙 하나가 게이트에서 후보를 통째로 버리고 있었다 —
  실부담비용을 못 찾고 총보수(합성총보수)만 확인되면 그 값을 넣고 `note` 첫머리에 "비용:총보수만"을 적게 바꿨다
  (위 "`cost_pct`의 총보수 폴백"). **코드 게이트는 `NA` 여부만 보므로 `gateCoreEvidence`는 그대로다.**
  대신 ③ 프롬프트가 그런 후보에 "비용과 추적 품질" 배점의 절반만 주고 `rationale`에 실부담비용 미확인을 적는다.
  ④ 대체 종목 후보 줄에 ① 표의 `aum_bn`·`cost_pct`·`turnover_bn`·`total_return_1y`·`price`를 실어
  "이 수치로 비교하고 웹 검색은 최신 공시 확인에만 쓴다"로 못 박았다(`ReplacementCandidate` 확장).
  `roleFromNote`는 note 앞 3조각을 훑어 자리를 찾도록 넓혔다 — "비용:총보수만"이 앞에 와도 자리를 놓치지 않는다.
  (`sys-watch.md`의 후보 줄에 보이던 `undefined`는 그 사본을 만든 합성 호출이 `category`에
  `Category` 밖 값을 넣어 `CATEGORY_LABEL` 조회가 빈 것이었다. 실DB 최신 ① 문서로 돌린 결과는 "자리 미정"이다.
  그래도 프롬프트에 `undefined`가 새지 않도록 블록에 카테고리 폴백과 `NA` 표기를 넣었다.)
  괴리율 문구를 "종가 기준 괴리율 5% 초과(LP 관리범위 밖, 당일 기준)"로 정리하고,
  50억 규칙 옆에 "KRX 요건은 신탁원본액과 순자산총액이 모두 50억 미만(반기말 기준)이며,
  여기서는 순자산만 보므로 더 보수적이다"를 붙였다. 출처 표기는
  **"기사(파이낸셜뉴스 2026-08-12)로 확인, 규정 원문 미확인"**으로 통일했다(①·②·④).
  **(7차에서 정정)** 그 기사는 괴리율 기사다 — 50억 요건의 출처는
  **"삼성자산운용 KODEX 가이드(KRX 상장규정 요약), 규정 원문 미확인"**이다. 괴리율 5%/10%의
  파이낸셜뉴스 출처는 그대로다(②의 괴리율 문장·④의 괴리율 문장).
  프롬프트는 ①②③④를 고쳤고 **⑤ 본문은 그대로다.**
  바꾸기 전 본문은 `data/tmp/prompt-backup-2026-09-21/{slot}.before-v8.md`, 최종본은
  `data/tmp/prompt-review-2026-09-21/{slot}.v8.md`(레포에 올리지 않음).
  시스템 지시문 사본(`sys-*.md`)은 이번 회차에도 갱신하지 않았다 — v6 시점 내용이다.
- 2026-09-21 (외부 검토 7차): **출처 정정과 비용 기준.**
  50억 요건에 붙어 있던 파이낸셜뉴스 출처를 **"삼성자산운용 KODEX 가이드(KRX 상장규정 요약),
  규정 원문 미확인"**으로 고쳤다(①·④). 그 기사는 괴리율 기사여서 엉뚱한 문장에 붙어 있었다 —
  괴리율 5%/10%의 파이낸셜뉴스 출처는 그대로다(②·④).
  비용 폴백을 **두 갈래**로 나눴다(위 "`cost_pct`의 비용 폴백과 비용 플래그") —
  `비용:합성총보수`(= 총보수 + 기타비용)와 `비용:총보수만`. ③ 채점 상한도 갈라
  총보수만은 20점의 절반, 합성총보수는 3/4으로 두고 "같은 기준끼리만 직접 비교"를 못 박았다.
  ④ 후보 줄의 비용 칸에 기준을 함께 찍는다(`costBasisFromNote` → `ReplacementCandidate.costBasis`,
  `비용 0.0062%(총보수만)` / `비용 0.0888%(합성총보수)` / `비용 0.23%`).
  `note` 앞부분의 순서(`자리 / 환헤지 / 비용 플래그`)를 형식 지시문과 ①② 프롬프트에 적었고,
  **파서는 그대로** `,` `/` `;` `|`를 모두 받는다.
  코드는 `docFormat.ts`(형식 지시문)·`watch.ts`(비용 기준) 2파일이고,
  회귀는 `check:watch`("비용 기준을 후보 줄에 표기한다")·`check:recommend`(형식 지시문 스냅샷).
  프롬프트는 ①②③④를 고쳤고 **⑤ 본문은 그대로다.**
  바꾸기 전 본문은 `data/tmp/prompt-backup-2026-09-21/{slot}.before-v9.md`, 최종본은
  `data/tmp/prompt-review-2026-09-21/{slot}.v9.md`(레포에 올리지 않음).
  시스템 지시문 사본(`sys-*.md`)은 이번 회차에도 갱신하지 않았다 — v6 시점 내용이다.
